# Complex integration test 2: function calls, while loop, error tracking
fn inc(x: i32):
    return x + 1

fn print_if_even(x: i32):
    if x % 2:
        print(0)
    else:
        print(x)

fn main():
    let i = 0
    while i < 5:
        print_if_even(i)
        i = inc(i)
    print(42)
