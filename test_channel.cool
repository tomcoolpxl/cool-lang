fn worker(ch: Channel[i32]) -> i32:
    let msg = ch.receive()
    print(msg)
    return 0

fn main() -> i32:
    let ch = Channel[i32](5)
    spawn worker(ch)
    ch.send(42)
    sleep(100)
    return 0
